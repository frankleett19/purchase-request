# ============================================================
# 三花生活館員購系統 — 環境變數範本
# 複製此檔為 .env（本機測試），或到 Netlify 後台
#   Site settings → Environment variables 逐項設定（部署用）。
# 注意：.env 內含密鑰，請勿 commit 進 Git（建議加入 .gitignore）。
# ============================================================

# 【必填】LINE Bot 的 Channel access token（long-lived）
# 取得：LINE Developers Console → 你的 Messaging API channel
#       → Messaging API 分頁 → Channel access token → Issue
LINE_CHANNEL_ACCESS_TOKEN=請貼上你的_channel_access_token

# 【選填】LINE Channel secret（目前推播不需要；若日後加 webhook 驗簽會用到）
LINE_CHANNEL_SECRET=

# 【選填】網站網址，用於 Flex 按鈕連結（預設已內建，可不填）
SITE_URL=https://stately-manatee-fc25ff.netlify.app

# 【選填】月底摘要 function 讀取資料用的 Apps Script /exec 網址
# 不填會用程式內建的預設值；若日後 GAS 重新部署網址有變，在這裡覆寫最方便
GAS_URL=https://script.google.com/macros/s/AKfycbx0INZrbtCitxj-ag0WoJFDVJOpW-DW2ZZrS6h9T8tGKFf8Mf_zcxaLA1Qvl39xIqW_7A/exec

# 【選填】每人每月員購額度（用於月摘要的「剩餘額度」）。0 = 不顯示剩餘額度
MONTHLY_QUOTA=0
